<aside class="menu">
  <h2 class="menu-label">
    Licences
  </h2>
  <ul class="menu-list">
    <li>
      <a
        <% if (headTitle === configFunctions.getConfigProperty("settings.licenceAliasPlural")) { %>
          class="is-active" href="#" aria-current="page"
        <% } else { %>
          href="<%= urlPrefix %>/licences"
        <% } %>
        >
        <span class="icon"><i class="fas fa-search" aria-hidden="true"></i></span>
        <%= configFunctions.getConfigProperty("settings.licenceAlias") %> Search
      </a>
    </li>
    <li>
      <a
        <% if (headTitle === configFunctions.getConfigProperty("settings.licenceAlias") + " Category Summary") { %>
          class="is-active" href="#" aria-current="page"
        <% } else { %>
          href="<%= urlPrefix %>/licences/licenceCategorySummary"
        <% } %>
        >
        <span class="icon"><i class="fas fa-table" aria-hidden="true"></i></span>
        <%= configFunctions.getConfigProperty("settings.licenceAlias") %> Category Summary
      </a>
    </li>
  </ul>
</aside>
